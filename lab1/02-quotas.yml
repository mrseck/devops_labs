---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: random-backend-quota
  namespace: random-backend
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "8Gi"
    limits.cpu: "8"
    limits.memory: "16Gi"
    pods: "10"
    services: "5"
    persistentvolumeclaims: "5"

---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: random-jobs-quota
  namespace: random-jobs
spec:
  hard:
    requests.cpu: "8"
    requests.memory: "16Gi"
    limits.cpu: "16"
    limits.memory: "32Gi"
    pods: "20"
    services: "5"
    persistentvolumeclaims: "3"

---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: random-db-quota
  namespace: random-db
spec:
  hard:
    requests.cpu: "2"
    requests.memory: "4Gi"
    limits.cpu: "4"
    limits.memory: "8Gi"
    pods: "5"
    services: "3"
    persistentvolumeclaims: "2"

---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: random-frontend-quota
  namespace: random-frontend
spec:
  hard:
    requests.cpu: "1"
    requests.memory: "2Gi"
    limits.cpu: "2"
    limits.memory: "4Gi"
    pods: "8"
    services: "1"
    persistentvolumeclaims: "1"

---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: random-scheduler-quota
  namespace: random-scheduler
spec:
  hard:
    requests.cpu: "1"
    requests.memory: "2Gi"
    limits.cpu: "2"
    limits.memory: "4Gi"
    pods: "3"
    services: "2"
    persistentvolumeclaims: "1"
